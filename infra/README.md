# Infra — schema, deploy notes, local tooling (no secrets)

- `supabase/migrations/` — PostgreSQL / Supabase schema for Hub
- Runtime secrets stay in GitHub Secrets or server env, never in this folder
