# Copy to frontend/.env.local (and optionally backend/.env) — never commit real values.
# Production: use GitHub Secrets / server env vars instead.

# OpenAI / LLM
OPENAI_API_KEY=

# Database
DATABASE_URL=

# Supabase (frontend)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
